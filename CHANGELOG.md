# Changelog

## 2026-05-11

### `web_app.py`

**新增**
- 新增轻量本地 Web 入口，运行 `python web_app.py` 后可在 `http://127.0.0.1:7860/` 上传 CSV 并生成台词本 TXT / SRT ZIP
- 支持从本地任意位置上传 CSV，不再要求输入文件位于 `input-csv/`
- 上传 CSV 后自动用文件名填充“输出名称”
- 支持预览 CSV、按集数筛选、默认只处理角色非空行
- 新增台词本高级设置：主角前缀、其他角色模板、连续台词连接符、每集标题模板、是否清理每句台词内部空格
- 新增 `n/9` 切换分隔符：台词本遇到以 `n` 或 `9` 开头的角色 marker 时，在新段落前插入空行和 `❖`
- 台词本显示角色时会去掉开头的 `n` / `9`，如 `n甲` 显示为 `【甲】`
- SRT 固定按同角色连续台词合并，连续台词用空格连接
- SRT 保留原始角色 marker，不插入台词本的 `❖` 分隔符

**修改**
- 下载输出改为手动保存：生成结果通过“下载台词本 TXT”和“下载 SRT ZIP”按钮下载，不自动写入项目目录
- 台词本预览改为显示完整内容，便于下载前检查

### 文档

**新增**
- 新增 `README-web.md`，单独说明 Web App 的启动方式、输入 CSV 格式、基础设置、高级设置、SRT 输出规则和下载行为

**修改**
- `README.md` 新增 `web_app.py` 入口说明，并链接到 `README-web.md`

## 2026-05-03

### `srt/process.py`

**删除**
- 移除 `rename_files` 函数，不再自动修改 SRT 文件名

**修复**
- `DIALOGUE_RE` 原要求 Actor/Effect 字段为空（`,,`），遇到有角色名的 ASS 文件时零匹配，静默生成空 SRT；改为 `[^,]*` 兼容任意字段内容
- 新增 `ASS_OVERRIDE_RE`，转换时剥除字幕中的 `{...}` 特效标签
- SRT 查找改用精确集数匹配 `(?<!\d){epno}(?!\d)`，避免 ep05 被误匹配为 ep15；找不到对应集数时报错退出

**新增**
- 启动时检测目录中的 `.ass` 文件，存在则提示 `是否转换为 .srt？[y/N]`，用户确认后才执行转换

### `menu.py`

**修改**
- 移除 `rename_files` 的 import 和调用
- 预处理字幕步骤改为：检测 `.ass` 文件 → 用 `questionary.confirm` 询问是否转换 → 确认后调用 `convert_ass_files` → 调用 `generate_csv`
